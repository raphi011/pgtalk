export const title = "Where a row lives";

# Where a row lives

## Pages, tuples, and why a table that lost rows got bigger

<Diagram cols={12} rows={2}>
  <Code x={6} y={1.1} sql="SELECT ctid, xmin, xmax, * FROM orders WHERE id = 1;" appearAt={1} />
</Diagram>
