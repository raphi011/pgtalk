export const fixture = "orders";
export const title = "The four questions on a real plan";
export const notes = `Key 1 turns parallelism off, as on the earlier slides: a parallel
plan puts a Gather Merge and three workers between them and the join, which is
a second lesson they have not had yet.
Then walk the four questions in order.
1. Innermost first: the two sequential scans, then the hash, then the join.
2. The join estimated 101,538 and got 101,307 — the planner was right here.
   Say so; a plan being correct is worth showing too.
3. Every loops count is 1, so the times are already wall time.
4. All buffers are hits, because the fixture was restored a moment ago and the
   table is still in shared_buffers. On a cold cache this plan looks very
   different, which is session 2.`;

# The four questions on a real plan

<Runnable appearAt={1} sql="SET max_parallel_workers_per_gather = 0;" />

<Plan
  analyze
  appearAt={2}
  sql={`SELECT c.country, count(*)
FROM customers c JOIN orders o ON o.customer_id = c.id
WHERE o.total_cents > 200000
GROUP BY c.country;`}
/>
