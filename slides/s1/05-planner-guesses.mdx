export const title = "What the planner actually does";
export const notes = `Three inputs, one output. Stress that none of the inputs is your data —
they are summaries of it, collected by ANALYZE, plus a handful of constants
someone chose in 1999.
The planner never looks at a row. It picks the cheapest plan under a model, and
"cheapest" is arithmetic on statistics, not measurement.
This is why a plan can be wrong while the planner is working perfectly.`;

# What the planner actually does

<Diagram cols={12} rows={5}>
  <Box id="stats" x={0} y={0} w={2.6} label="Statistics" sub="pg_statistic" appearAt={1} />
  <Box id="cost"  x={0} y={1.6} w={2.6} label="Cost constants" sub="seq_page_cost, …" appearAt={2} />
  <Box id="shape" x={0} y={3.2} w={2.6} label="Candidate plans" sub="scans, joins, orders" appearAt={3} />

  <Box id="model" x={4.5} y={1.6} w={3} label="Cost model" sub="arithmetic, no I/O" appearAt={4} tone="accent" />
  <Box id="plan"  x={9} y={1.6} w={3} label="Chosen plan" sub="lowest total cost" appearAt={6} />

  <Arrow from="stats" to="model" appearAt={4} />
  <Arrow from="cost" to="model" appearAt={4} />
  <Arrow from="shape" to="model" appearAt={4} />
  <Arrow from="model" to="plan" appearAt={6} />

  <Label x={6} y={4.6} text="never reads a single row" appearAt={5} />
</Diagram>
