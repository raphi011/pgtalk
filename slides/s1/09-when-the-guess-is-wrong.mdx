export const fixture = "orders";
export const title = "When the guess is wrong";
export const notes = `This is the slide to slow down on.
id % 7 = 0 selects one row in seven — 14% of the table. The planner has
statistics on id, but none on id % 7, so it falls back to a default guess for
an opaque expression: 0.5%.
Estimate 2,500 rows. Actual 71,428. The tree marks it 29x off.
On a lone count that costs nothing. Put this expression on one side of a join
and the planner picks a nested loop for 2500 rows that has to run 71000 times.
That is how a query goes from 40 ms to four minutes without anything "being
slow".
The fix is not a hint. It is giving the planner statistics it can use:
CREATE STATISTICS, or an expression index, or storing the value as a column.`;

# When the guess is wrong

<Runnable appearAt={1} sql="SET max_parallel_workers_per_gather = 0;" />

The planner has statistics on `id`. It has none on `id % 7`, so it falls back
to a fixed guess for expressions it cannot see through.

<Plan analyze sql="SELECT count(*) FROM orders WHERE id % 7 = 0;" appearAt={2} />

**Predict.** One row in seven matches. What fraction will the planner assume?
