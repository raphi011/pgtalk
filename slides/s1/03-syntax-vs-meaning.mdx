export const fixture = "orders";
export const title = "Two different failures";
export const notes = `Run both (keys 1 and 2).
The first never reaches the catalog: it dies on grammar, and PostgreSQL can
only point at a character position.
The second parses perfectly and dies on meaning — and because the analyzer has
the catalog open, it can suggest what you meant. That hint is the catalog
talking.
Different stage, different SQLSTATE: 42601 vs 42703.`;

# Two different failures

Syntax belongs to the parser. Meaning belongs to the analyzer. They fail in
different stages, and PostgreSQL tells you which one it was.

<Runnable sql="SELECT FROM WHERE customers;" appearAt={1} />

<Runnable sql="SELECT nope FROM customers;" appearAt={2} />
